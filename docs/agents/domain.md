# Domain documentation

This is a single-context repository. Read `/CONTEXT.md` before changing product behavior. Architecture decisions live in `/docs/adr/`; add an ADR when a durable interface, persistence schema, or dependency direction changes.
